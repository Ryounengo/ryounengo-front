const HOME_PAGE_ROUTE = "/";
const DECKS_PAGE_ROUTE = "decks";
const CREATE_DECK_PAGE_ROUTE = `${DECKS_PAGE_ROUTE}/create`;

const DECK_ROUTES = {
    CREATE_DECK_PAGE_ROUTE,
};

export const ROUTES = {
    HOME_PAGE_ROUTE,
    DECK_ROUTES,
};
